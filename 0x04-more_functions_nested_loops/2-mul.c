#include "main.h"

/**
 *mul -function that multiplies two integers
 *@x: first integer to multiply
 *@y: second integer to multiply with the first
 *Return: result of the multiplication
 */

int mul(int x, int y)
{
	return (x * y);
}
