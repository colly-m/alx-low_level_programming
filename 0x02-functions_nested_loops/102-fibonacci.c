#include <stdio.h>
/**
 *main - prints the first 52 fibonacci numbers
 *Return: Nothing!
 */
int main(void)
{
	int count;
	long i = 0, j = 1, sum;

	for (count = 0; count < 50; count++)
	{
		sun = i + j
		printf("%ld", sum);

		i = j;
		j = sum;

		if (count == 49)
			printf("\n");
		else
			printf(",");
	}
	return (0);
}
